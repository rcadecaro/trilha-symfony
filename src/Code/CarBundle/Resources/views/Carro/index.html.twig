{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Carro list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Modelo</th>
                <th>Ano</th>
                <th>Cor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('carro_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.ano }}</td>
                <td>{{ entity.cor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('carro_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('carro_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('carro_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
